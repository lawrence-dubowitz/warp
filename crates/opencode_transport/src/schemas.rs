//! Vendored opencode wire schemas (events, sessions, parts, tools).
//!
//! Populated incrementally as the backend wires up. Keep these mirroring
//! upstream opencode types; do not let Warp domain types leak in here.
